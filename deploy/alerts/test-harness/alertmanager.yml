route:
  receiver: webhook-sink
  group_by: [alertname, source, target, protocol]
  group_wait: 0s
  group_interval: 1s
  repeat_interval: 1m

receivers:
  - name: webhook-sink
    webhook_configs:
      - url: http://webhook-sink:8080/alerts
        send_resolved: true
