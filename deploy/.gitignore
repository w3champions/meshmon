# Alertmanager webhook secrets — never commit credentials to version control.
# Only the placeholder is tracked. The ** pattern ensures nested
# subdirectories (e.g. alertmanager/secrets/prod/webhook) are also ignored
# recursively.
alertmanager/secrets/**
!alertmanager/secrets/.gitkeep
